apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: clustergate-leader-election
  namespace: clustergate-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: clustergate-leader-election
subjects:
  - kind: ServiceAccount
    name: clustergate-controller
    namespace: clustergate-system
