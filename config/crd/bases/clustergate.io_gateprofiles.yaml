---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: gateprofiles.clustergate.io
spec:
  group: clustergate.io
  names:
    kind: GateProfile
    listKind: GateProfileList
    plural: gateprofiles
    shortNames:
    - gp
    singular: gateprofile
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GateProfile is the Schema for the gateprofiles API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GateProfileSpec defines the desired state of GateProfile.
            properties:
              checks:
                description: Checks is the list of check references included in this
                  profile.
                items:
                  description: ProfileCheckRef is a reference to a built-in or dynamic
                    check within a GateProfile.
                  properties:
                    category:
                      description: Category overrides the check's default category.
                      type: string
                    config:
                      description: Config holds check-specific configuration as arbitrary
                        JSON.
                      x-kubernetes-preserve-unknown-fields: true
                    enabled:
                      description: Enabled controls whether this check is active.
                      type: boolean
                    gateCheckRef:
                      description: |-
                        GateCheckRef references a GateCheck CR by metadata.name.
                        Mutually exclusive with Name.
                      type: string
                    interval:
                      description: Interval overrides the default check interval.
                      type: string
                    name:
                      description: |-
                        Name is the identifier for a built-in check (e.g. "dns").
                        Mutually exclusive with GateCheckRef.
                      type: string
                    severity:
                      description: Severity overrides the check's default severity.
                      enum:
                      - critical
                      - warning
                      - info
                      type: string
                  type: object
                type: array
              description:
                description: Description is a human-readable description of this profile.
                type: string
            required:
            - checks
            type: object
          status:
            description: GateProfileStatus defines the observed state of GateProfile.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the GateProfile's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
