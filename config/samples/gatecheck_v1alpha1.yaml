apiVersion: clustergate.io/v1alpha1
kind: GateCheck
metadata:
  name: istiod-ready
spec:
  description: "Verifies istiod control plane pods are running — extend baseline with service mesh readiness"
  severity: warning
  category: networking
  podCheck:
    namespace: istio-system
    labelSelector:
      matchLabels:
        app: istiod
    minReady: 2
