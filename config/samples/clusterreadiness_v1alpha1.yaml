apiVersion: clustergate.io/v1alpha1
kind: ClusterReadiness
metadata:
  name: production-readiness
spec:
  interval: 60s
  # Reference a profile for baseline checks
  profiles:
    - name: production-baseline
  # Only add cluster-specific checks inline — profile handles the baseline
  # checks:
  #   - gateCheckRef: istiod-ready
  #     severity: critical
  #     category: networking
  #   - gateCheckRef: vault-health
  #     severity: warning
  #     category: security
