apiVersion: clustergate.io/v1alpha1
kind: ClusterReadiness
metadata:
  name: default
spec:
  interval: 60s
  # Baseline: control plane checks ensure the cluster is fundamentally healthy
  profiles:
    - name: control-plane
  # Extend with platform-specific checks as needed:
  # checks:
  #   - gateCheckRef: istiod-ready
  #     severity: warning
  #     category: networking
  #   - gateCheckRef: vault-health
  #     severity: warning
  #     category: security
